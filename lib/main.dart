import 'package:flutter/material.dart';
import 'package:mydatabasesqlite/ui/listview_employees.dart';

void main() => runApp(
      MaterialApp(
        title: 'Returning Data',
        home: ListViewEmployees(),
      ),
    );
